# Diun Notifier Docker Containers

### Test notifications

Through the command line with:

```markdown
diun notif test
```

Or within a container:

```markdown
docker compose exec diun diun notif test
```





